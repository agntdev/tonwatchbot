{
  "name": "tonwatchbot-bot",
  "version": "0.1.0",
  "private": true,
  "description": "TonWatchBot — Telegram bot for TON/jetton price watching and alerts (AGNTDEV-generated, built on @agntdev/bot-toolkit)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node ./scripts/test-gate.mjs"
  },
  "dependencies": {
    "@agntdev/bot-toolkit": "file:./.agntdev-bot-toolkit.tgz",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
